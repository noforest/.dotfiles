
# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hyprland.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor=,preferred,auto,auto
# monitor=eDP-1,1920x1080@60,0x0,1
# monitor = eDP-1,preferred,auto,1

# Définir HDMI-A-1 comme écran principal à gauche
monitor=HDMI-A-1,preferred,0x0,1
# Positionner eDP-1 à droite de HDMI (2560px de large pour le Xiaomi et 200 d'offset y)
monitor=eDP-1,1920x1080@60,2560x200,1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = alacritty
$fileManager = thunar
$menu = wofi --show drun -n


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = hyprpm reload --notify-fail
exec-once = hyprctl dispatch focusmonitor HDMI-A-1
# exec-once = ~/.local/bin/scripts/app_per_workspace_startup.sh
exec-once = waybar
exec-once = swaync
exec-once = swaync-client -df
exec-once = swww-daemon
# exec-once = ~/test_startup.sh

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24


###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
permission = /usr/(bin|local/bin)/hyprpm, plugin, allow
# permission = /sbin/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 20

    border_size = 1

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    # col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = master
    # layout = dwindle
}


# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = false
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# DEFAULT
# https://wiki.hyprland.org/Configuring/Variables/#animations
# animations {
#     enabled = yes, please :)
#
#     # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
#
#     bezier = easeOutQuint,0.23,1,0.32,1
#     bezier = easeInOutCubic,0.65,0.05,0.36,1
#     bezier = linear,0,0,1,1
#     bezier = almostLinear,0.5,0.5,0.75,1.0
#     bezier = quick,0.15,0,0.1,1
#
#     animation = global, 1, 10, default
#     animation = border, 1, 5.39, easeOutQuint
#     animation = windows, 1, 4.79, easeOutQuint
#     animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
#     animation = windowsOut, 1, 1.49, linear, popin 87%
#     animation = fadeIn, 1, 1.73, almostLinear
#     animation = fadeOut, 1, 1.46, almostLinear
#     animation = fade, 1, 3.03, quick
#     animation = layers, 1, 3.81, easeOutQuint
#     animation = layersIn, 1, 4, easeOutQuint, fade
#     animation = layersOut, 1, 1.5, linear, fade
#     animation = fadeLayersIn, 1, 1.79, almostLinear
#     animation = fadeLayersOut, 1, 1.39, almostLinear
#     animation = workspaces, 1, 1.94, almostLinear, fade
#     animation = workspacesIn, 1, 1.21, almostLinear, fade
#     animation = workspacesOut, 1, 1.94, almostLinear, fade
# }


# BALAYAGE
animations {
    enabled = true

    # bezier=workspace, 1, 0, 0, 1  # instantané
    # animation=workspaces, 1, 0, workspace

    # Animation curves

    bezier = linear, 0, 0, 1, 1
    bezier = md3_standard, 0.2, 0, 0, 1
    bezier = md3_decel, 0.05, 0.7, 0.1, 1
    bezier = md3_accel, 0.3, 0, 0.8, 0.15
    bezier = overshot, 0.05, 0.9, 0.1, 1.1
    bezier = crazyshot, 0.1, 1.5, 0.76, 0.92 
    bezier = hyprnostretch, 0.05, 0.9, 0.1, 1.0
    bezier = menu_decel, 0.1, 1, 0, 1
    bezier = menu_accel, 0.38, 0.04, 1, 0.07
    bezier = easeInOutCirc, 0.85, 0, 0.15, 1
    bezier = easeOutCirc, 0, 0.55, 0.45, 1
    bezier = easeOutExpo, 0.16, 1, 0.3, 1
    bezier = softAcDecel, 0.26, 0.26, 0.15, 1
    bezier = md2, 0.4, 0, 0.2, 1 # use with .2s duration
    # Animation configs
    animation = windows, 1, 3, md3_decel, popin 60%
    animation = windowsIn, 1, 3, md3_decel, popin 60%
    animation = windowsOut, 1, 3, md3_accel, popin 60%
    animation = border, 1, 10, default
    animation = fade, 1, 3, md3_decel
    # animation = layers, 1, 2, md3_decel, slide
    animation = layersIn, 1, 3, menu_decel, slide
    animation = layersOut, 1, 1.6, menu_accel
    animation = fadeLayersIn, 1, 2, menu_decel
    animation = fadeLayersOut, 1, 0.5, menu_accel
    animation = workspaces, 1, 7, menu_decel, slide
    # animation = workspaces, 1, 2.5, softAcDecel, slide
    # animation = workspaces, 1, 7, menu_decel, slidefade 15%
    # animation = specialWorkspace, 1, 3, md3_decel, slidefadevert 15%
    animation = specialWorkspace, 1, 3, md3_decel, slidevert
    # animation = specialWorkspace, 1, 5, smoothOut, slide
}

# REDDIT
# animations {
# # https://cubic-bezier.com/
# # https://easings.net
# # https://https://www.cssportal.com/css-cubic-bezier-generator/
#
#     enabled = true
#         first_launch_animation = true
#
#         bezier = wind, 0.05, 0.9, 0.1, 1.05
#         bezier = winIn, 0.1, 1.1, 0.1, 1.1
#         bezier = winOut, 0.3, -0.3, 0, 1
#         bezier = linear, 1, 1, 1, 1
#         bezier = Cubic, 0.1, 0.1, 0.1, 1
#         bezier = overshot, 0.05, 0.9, 0.1, 1.1
#         bezier = ease-in-out, 0.17, 0.67, 0.83, 0.67
#         bezier = ease-in, 0.17, 0.67, 0.83, 0.67
#         bezier = ease-out, 0.42, 0, 1, 1
#         bezier = easeInOutSine, 0.37, 0, 0.63, 1
#         bezier = easeInSine, 0.12, 0, 0.39, 0
#         bezier = easeOutSine, 0.61, 1, 0.88, 1
#
#         animation = windowsIn, 1, 3, easeInOutSine, popin
#         animation = windowsOut, 1, 3, easeInOutSine, popin
#
#         animation = border, 1, 3, easeInOutSine
#         animation = borderangle, 1, 30, easeInOutSine, loop
#
#         animation = workspacesIn, 1, 3, easeInOutSine, slidefade
#         animation = workspacesOut, 1, 3, easeInOutSine, slidefade
#
#         animation = specialWorkspaceIn, 1, 3, easeInOutSine, slidevert
#         animation = specialWorkspaceOut, 1, 3, easeInOutSine, slidevert
#
#         animation = layersIn, 1, 3, easeInOutSine, fade
#         animation = layersOut, 1, 3, easeInOutSine, fade
# }

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
# dwindle {
#     pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
#     preserve_split = true # You probably want this
# }
dwindle {
    preserve_split = true
    smart_split = false
    smart_resizing = false
}


# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    mfact = 0.5
    new_status = slave
    new_on_active = after
    new_on_top = true
    allow_small_split = true
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = fr
    kb_variant =
    kb_model =
    kb_options = caps:escape
    kb_rules =

    repeat_delay = 250      # délai avant répétition (en ms)
    repeat_rate = 50        # répétitions par seconde


    follow_mouse = 1
    sensitivity = 0.0 # -1.0 - 1.0, 0 means no modification.

    accel_profile =  adaptive

    touchpad {
        natural_scroll = true
        # scroll_factor = 0.7
        scroll_factor = 0.9
    }

}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = true
    workspace_swipe_distance = 700
    workspace_swipe_fingers = 4
    workspace_swipe_cancel_ratio = 0.2
    workspace_swipe_min_speed_to_force = 5
    workspace_swipe_direction_lock = true
    workspace_swipe_direction_lock_threshold = 10
    workspace_swipe_create_new = false
}


plugin {
    split-monitor-workspaces {
        count = 9
        keep_focused = 1
        enable_notifications = 0
        enable_persistent_workspaces = 1
    }

    # hyprsplit {
    #     num_workspaces = 9
    #     persistent_workspaces = false
    # }
}



# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
# device {
#     name = epic-mouse-v1
#     sensitivity = -0.5
# }

###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
# Programmes
bind = $mainMod, p, exec, $menu
bind = $mainMod SHIFT, Return, exec, $terminal
# bind = $mainMod, b, exec, hyprctl dispatch toggleopaque # togglebar n'existe pas -> comportement simulé
bind = $mainMod|SHIFT, c, killactive, 



# changer le focus et bouger les fenêtres
bind = $mainMod, j, layoutmsg, cyclenext
bind = $mainMod, k, layoutmsg, cycleprev
bind = $mainMod SHIFT, j, layoutmsg, swapnext
bind = $mainMod SHIFT, k, layoutmsg, swapprev


# Modifier taille des fenêtres
binde = $mainMod, LEFT, resizeactive, -20 0
binde = $mainMod, RIGHT, resizeactive, 20 0
binde = $mainMod, UP, resizeactive, 0 -20
binde = $mainMod, DOWN, resizeactive, 0 20


bind = $mainMod, y, layoutmsg, orientationcycle top left

# Reset layout après avoir trop modifié la taille des fenêtres et tout
# ????????????????

# Mode switching 
bind = $mainMod, t, togglefloating, 
bind = $mainMod, f, fullscreen, # toggle -> il faut refaire ce raccourci pour s'enleverdu mode
bind = $mainMod, m, fullscreen, # toggle -> il faut refaire ce raccourci pour s'enleverdu mode

# Tab (view)
bind = $mainMod, Tab, workspace, previous

# Change monitor focus
bind = $mainMod, comma, focusmonitor, -1
bind = $mainMod, semicolon, focusmonitor, 1

# SANS plugin : https://github.com/Duckonaut/split-monitor-workspaces (injecté avec hyprpm)
# bind = $mainMod SHIFT, comma, exec, ~/.local/bin/scripts/toggle_window_between_two_monitors.sh

# # AVEC plugin
bind = $mainMod SHIFT, comma, split-changemonitor, next

# Quit
bind = $mainMod SHIFT, q, exit,


# Screenshots
bind = $mainMod SHIFT, s, exec, grim -g "$(slurp)" - | convert - -shave 1x1 PNG:- | swappy -f -

# Apps
bind = $mainMod, g, exec, firefox -P hyprland
bind = $mainMod SHIFT, asterisk, exec, custom_dir_open_alacritty.sh
bind = $mainMod, s, exec, spotify
bind = $mainMod, o, exec, obsidian
bind = $mainMod, e, exec, thunar
bind = $mainMod SHIFT, l, exec, wlogout --buttons-per-row=2
bind = $mainMod, w, exec, wallpaper_hyprland.sh


# Workspaces [0–9] (numérique) - SANS PLUGIN : https://github.com/Duckonaut/split-monitor-workspaces (injecté avec hyprpm)
# bind = $mainMod, ampersand, workspace, 1
# bind = $mainMod, eacute, workspace, 2
# bind = $mainMod, quotedbl, workspace, 3
# bind = $mainMod, apostrophe, workspace, 4
# bind = $mainMod, parenleft, workspace, 5
# bind = $mainMod, minus, workspace, 6
# bind = $mainMod, egrave, workspace, 7
# bind = $mainMod, underscore, workspace, 8
# bind = $mainMod, ccedilla, workspace, 9
#
# bind = $mainMod SHIFT, ampersand, movetoworkspace, 1
# bind = $mainMod SHIFT, eacute, movetoworkspace, 2
# bind = $mainMod SHIFT, quotedbl, movetoworkspace, 3
# bind = $mainMod SHIFT, apostrophe, movetoworkspace, 4
# bind = $mainMod SHIFT, parenleft, movetoworkspace, 5
# bind = $mainMod SHIFT, minus, movetoworkspace, 6
# bind = $mainMod SHIFT, egrave, movetoworkspace, 7
# bind = $mainMod SHIFT, underscore, movetoworkspace, 8
# bind = $mainMod SHIFT, ccedilla, movetoworkspace, 9
#
# Shiftview
# binde = $mainMod, z, workspace, e+1
# binde = $mainMod, a, workspace, e-1











# AVEC PLUGIN
bind = $mainMod, ampersand, split-workspace, 1
bind = $mainMod, eacute, split-workspace, 2
bind = $mainMod, quotedbl, split-workspace, 3
bind = $mainMod, apostrophe, split-workspace, 4
bind = $mainMod, parenleft, split-workspace, 5
bind = $mainMod, minus, split-workspace, 6
bind = $mainMod, egrave, split-workspace, 7
bind = $mainMod, underscore, split-workspace, 8
bind = $mainMod, ccedilla, split-workspace, 9

bind = $mainMod SHIFT, ampersand, split-movetoworkspace, 1
bind = $mainMod SHIFT, eacute, split-movetoworkspace, 2
bind = $mainMod SHIFT, quotedbl, split-movetoworkspace, 3
bind = $mainMod SHIFT, apostrophe, split-movetoworkspace, 4
bind = $mainMod SHIFT, parenleft, split-movetoworkspace, 5
bind = $mainMod SHIFT, minus, split-movetoworkspace, 6
bind = $mainMod SHIFT, egrave, split-movetoworkspace, 7
bind = $mainMod SHIFT, underscore, split-movetoworkspace, 8
bind = $mainMod SHIFT, ccedilla, split-movetoworkspace, 9

# Shiftview
binde = $mainMod, z, split-cycleworkspaces, next
binde = $mainMod, a, split-cycleworkspaces, prev

bind = $mainMod SHIFT, g, split-grabroguewindows


# Example special workspace (scratchpad)
# bind = $mainMod, S, togglespecialworkspace, magic
# bind = $mainMod SHIFT, S, movetoworkspace, special:magic
bind = $mainMod, SPACE, togglespecialworkspace, magic
bind = $mainMod SHIFT, SPACE, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
# bind = $mainMod, mouse_down, workspace, e+1
# bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl set 15+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl set 15-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

# SANS PLUGIN: split monitor
# workspace=1,monitor:HDMI-A-1
# workspace=2,monitor:eDP-1
# # Fallback si HDMI-A-1 n'est pas branché
# workspace=1,monitor:eDP-1


# workspace = 1, monitor:HDMI-A-1
# workspace = 2, monitor:eDP-1
# workspace = 3, monitor:HDMI-A-1
# workspace = 4, monitor:HDMI-A-1
# workspace = 5, monitor:HDMI-A-1
# workspace = 6, monitor:HDMI-A-1
# workspace = 7, monitor:HDMI-A-1
# workspace = 8, monitor:HDMI-A-1
# workspace = 9, monitor:HDMI-A-1
# layerrule = blur, waybar
layerrule = ignorezero, waybar
layerrule = ignorealpha 0.7, waybar
# layerrule = blur, swaync-control-center
# layerrule = blur, swaync-notification-window
# layerrule = ignorezero, swaync-control-center
# layerrule = ignorezero, swaync-notification-window
# layerrule = ignorealpha 0.5, swaync-control-center
# layerrule = ignorealpha 0.5, swaync-notification-window
#
# layerrule = blur, wofi
layerrule = ignorezero, wofi
layerrule = ignorealpha 0.9, wofi
