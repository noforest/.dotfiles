#!/bin/sh

case $BUTTON in
    1) 
        calendar=$(cal | sed "s/\b$(date +'%e' | sed 's/ //g')\b/<b><span color='red'>&<\/span><\/b>/")
        notify-send "This Month" "$calendar"
        ;;
esac

clock2=$(date "+%a, %H:%M")
echo "$clock2"

